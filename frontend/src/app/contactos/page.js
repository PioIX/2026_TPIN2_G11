export default function ListaContactos() {
    return (
        <main>
            {/* <ul>
                {contactos.map((msg, index) => (
                    <li key={`${msg.room}-${index}`}>
                        {msg.message}
                    </li>
                ))}
            </ul> */}
        </main>

    )
}